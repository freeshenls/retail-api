// app/javascript/controllers/finance/order_unsettled_component_controller.js
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  connect() {
    // 可以在这里初始化表格的特殊交互，如多选框等
  }
}
