<div class="space-y-6 bg-slate-50 min-h-screen no-italic font-sans" data-controller="order--index-component">
  <%= query %>

  <%# --- 2. 列表展示区 --- %>
  <div class="bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden">
    <%= table %>
    <%= pagy %>
  </div>
</div>
