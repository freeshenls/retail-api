<div class="w-full space-y-4">
  <%# 顶部导航栏 - 样式与 Index 保持高度统一 %>
  <div class="flex items-center justify-between bg-white border border-slate-200 rounded-xl px-4 py-3 shadow-sm">
    <div class="flex items-center gap-4">
      <%= link_to helpers.designer_drafts_path, 
                  data: { turbo_frame: "main_content" },
                  class: "p-2 hover:bg-slate-100 rounded-full transition-colors text-slate-500" do %>
        <%= heroicon "arrow-left", options: { class: "h-5 w-5" } %>
      <% end %>
      <div class="h-8 w-px bg-slate-200 mx-1"></div>
      <div>
        <h1 class="text-sm font-bold text-slate-900 truncate max-w-[200px]"><%= @draft.name %></h1>
        <p class="text-[11px] text-slate-500 font-mono"><%= @draft.id %></p>
      </div>
    </div>

    <div class="flex items-center gap-3">
      <%= link_to "返回列表", helpers.designer_drafts_path, 
                  data: { turbo_frame: "main_content" },
                  class: "px-4 py-2 text-slate-600 text-sm font-bold hover:text-slate-900 transition" %>
      
      <%# 这里的按钮可以是一个跳转链接，去往真正的下单表单页面 %>
      <%= link_to "查看订单", "#", 
                  class: "px-6 py-2 bg-[#0066b3] text-white text-sm font-bold rounded-lg hover:bg-[#005596] shadow-sm transition-all" %>
    </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-4 gap-4">
    <%# 主展示区 %>
    <div class="lg:col-span-3 space-y-4">
      <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden">
        <div class="px-5 py-3 border-b border-slate-100 bg-slate-50/50 flex justify-between items-center">
          <span class="text-xs font-bold text-slate-600 italic">PREVIEW / 稿件预览</span>
        </div>
        
        <div class="p-12 flex flex-col items-center justify-center bg-slate-50/20 min-h-[400px]">
          <div class="relative group">
            <div class="p-10 bg-white rounded-3xl shadow-xl border border-slate-100 transform group-hover:scale-105 transition-transform duration-500">
              <%= heroicon "document-text", options: { class: "h-24 w-24 text-slate-200" } %>
            </div>
            <%# 右下角类型小标签 %>
            <span class="absolute -bottom-2 -right-2 px-3 py-1 bg-slate-800 text-white text-[10px] font-bold rounded-lg uppercase tracking-widest shadow-lg">
              <%= @draft.try(:file_type)&.split('/')&.last || 'FILE' %>
            </span>
          </div>
          <p class="mt-8 text-slate-400 text-sm font-medium italic">此稿件已通过系统校验，可直接下单</p>
        </div>
      </div>
    </div>

    <%# 侧边信息卡片 %>
    <div class="lg:col-span-1 space-y-4">
      <div class="bg-white border border-slate-200 rounded-xl shadow-sm p-6">
        <h3 class="text-xs font-bold text-slate-400 uppercase tracking-[0.1em] mb-6">File Details</h3>
        
        <div class="space-y-6">
          <div class="group">
            <label class="text-[10px] text-slate-400 block mb-1 font-bold uppercase">文件名</label>
            <p class="text-sm font-bold text-slate-800 break-all leading-relaxed"><%= @draft.name %></p>
          </div>

          <div class="grid grid-cols-2 gap-4">
            <div>
              <label class="text-[10px] text-slate-400 block mb-1 font-bold uppercase">大小</label>
              <p class="text-sm font-bold text-slate-700"><%= file_size_human %></p>
            </div>
            <div>
              <label class="text-[10px] text-slate-400 block mb-1 font-bold uppercase">类型</label>
              <p class="text-sm font-bold text-slate-700 uppercase"><%= @draft.try(:file_type)&.split('/')&.last %></p>
            </div>
          </div>

          <div class="pt-4 border-t border-slate-50">
            <label class="text-[10px] text-slate-400 block mb-1 font-bold uppercase">上传于</label>
            <p class="text-xs font-bold text-slate-600"><%= format_time(@draft.created_at) %></p>
          </div>
        </div>
      </div>

      <%# 底部状态提示 - 品牌蓝风格 %>
      <div class="bg-[#0066b3] rounded-xl shadow-lg p-5 text-white/90">
        <div class="flex items-center gap-2 mb-2">
          <%= heroicon "information-circle", options: { class: "h-4 w-4 text-orange-400" } %>
          <span class="text-[11px] font-bold uppercase tracking-wider">温馨提示</span>
        </div>
        <p class="text-[11px] leading-relaxed">
          稿件上传后进入自动化订单流程，请在待提交订单页补充印刷排版内容。
        </p>
      </div>
    </div>
  </div>
</div>
