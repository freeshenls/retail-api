<%# app/views/admin/service_types/edit.html.erb %>
<%= render layout: "admin/layout" do %>
  <div data-controller="layout--tab-init" 
       data-layout--tab-init-label-value="服务定价-<%= @service_type.service_type%>" 
       data-layout--tab-init-path-value="<%= request.path %>">
  <%# 这里的代码会注入到 layout 的 yield 位置，也就是 main_content 内部 %>
    <%= render Admin::ServiceTypeEditComponent.new(service_type: @service_type) %>
  </div>
<% end %>
